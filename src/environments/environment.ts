export const environment = {
  production: true,
  // Point this at your deployed starlite-api instance, or serve this app
  // behind the same reverse proxy as the API and use a relative '/api/v2'.
  apiBaseUrl: 'http://localhost:8080/api/v2',
};
